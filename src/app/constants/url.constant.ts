import { environment } from "../environments/environment";
// import { environment } from "../environments/environment.prod";

export const USERS = environment.apiUrl + 'users';
export const EMAILS = environment.apiUrl + 'emails';
export const FORMS = environment.apiUrl + 'forms';
export const MERCHANT = environment.apiUrl + 'merchant';
export const POS = environment.apiUrl + 'POS';